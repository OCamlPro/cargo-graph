; generated by drom from package skeleton 'library'

(library
  (name cargo_graph_lib)
  (public_name cargo_graph_lib)
  (wrapped true)
  (libraries toml ez_file ez_dot )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package cargo_graph_lib))


